<!-- STUB — replace this with the real project README. It exists only so the
     template's landing page isn't blank. Tooling docs live in .config/README.md. -->

# Project Name

> Scaffolded from the [Template](https://github.com/DenWin/Template) repo.
> Replace this file with your project's README.

## Getting started

After cloning, set up the local dev tooling once:

```pwsh
pwsh -NoProfile -File .config/scripts/Initialize-DevEnvironment.ps1
```

For what the tooling does and why it's shaped this way, see
[.config/README.md](.config/README.md).
<!-- setup-teardown:template-only:start -->
Migrating this tooling into a repo that isn't scaffolded from this template?
See [setup/MIGRATION.md](setup/MIGRATION.md).
<!-- setup-teardown:template-only:end -->
